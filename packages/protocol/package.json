{
  "name": "@cliptown/protocol",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "index.d.ts",
  "types": "index.d.ts",
  "scripts": {
    "build": "test -f dist/SchemaVersion.ts || (echo 'Run pnpm build:rust first' && exit 1)",
    "test": "echo no-op"
  }
}
